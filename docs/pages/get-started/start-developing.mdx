import React from "react";
import { View, Text, Image } from "react-native";
import { Button } from "@/components/ui/button";
import { NavigationContainer } from "@react-navigation/native";
import { createStackNavigator } from "@react-navigation/stack";

const Stack = createStackNavigator();

// Tela Inicial
function HomeScreen({ navigation }) {
  return (
    <View className="flex-1 items-center justify-center bg-black px-6">
      {/* Logo */}
      <Image
        source={{
          uri: "https://cdn.openai.com/file-A_vector-based_logo_for_Studio_Solubro_Vision.png",
        }}
        style={{ width: 200, height: 200, resizeMode: "contain" }}
      />

      {/* Título */}
      <Text className="text-yellow-500 text-2xl font-bold mt-6 text-center">
        Studio Solubro Vision
      </Text>

      {/* Subtítulo */}
      <Text className="text-yellow-400 text-base mt-2 text-center">
        Qualidade Ampliada
      </Text>

      {/* Botões principais */}
      <View className="mt-10 w-full gap-4">
        <Button
          className="bg-yellow-500 rounded-2xl p-4 w-full"
          onPress={() => navigation.navigate("Portfolio")}
        >
          <Text className="text-black font-semibold text-lg">Ver Portfólio</Text>
        </Button>

        <Button
          className="bg-yellow-500 rounded-2xl p-4 w-full"
          onPress={() => navigation.navigate("Agendamento")}
        >
          <Text className="text-black font-semibold text-lg">
            Agendar Ensaio
          </Text>
        </Button>

        <Button
          className="bg-yellow-500 rounded-2xl p-4 w-full"
          onPress={() => navigation.navigate("Galeria")}
        >
          <Text className="text-black font-semibold text-lg">Minha Galeria</Text>
        </Button>
      </View>
    </View>
  );
}

// Tela Portfólio
function PortfolioScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-black px-6">
      <Text className="text-yellow-500 text-xl font-bold">Portfólio</Text>
      <Text className="text-white mt-2 text-center">
        Aqui você verá os trabalhos realizados pelo estúdio.
      </Text>
    </View>
  );
}

// Tela Agendamento
function AgendamentoScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-black px-6">
      <Text className="text-yellow-500 text-xl font-bold">Agendar Ensaio</Text>
      <Text className="text-white mt-2 text-center">
        Aqui o cliente poderá marcar ensaios fotográficos ou vídeos.
      </Text>
    </View>
  );
}

// Tela Galeria
function GaleriaScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-black px-6">
      <Text className="text-yellow-500 text-xl font-bold">Minha Galeria</Text>
      <Text className="text-white mt-2 text-center">
        Área exclusiva para cada cliente acessar suas fotos e vídeos.
      </Text>
    </View>
  );
}

export default function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator
        screenOptions={{
          headerShown: true,
          headerStyle: { backgroundColor: "#000" },
          headerTintColor: "#FFD700",
        }}
      >
        <Stack.Screen name="Início" component={HomeScreen} />
        <Stack.Screen name="Portfolio" component={PortfolioScreen} />
        <Stack.Screen name="Agendamento" component={AgendamentoScreen} />
        <Stack.Screen name="Galeria" component={GaleriaScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}
